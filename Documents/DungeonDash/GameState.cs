// ------------------------------------------------------------------
// GameState — Enum for the game's state machine
// Controls which screen/logic runs each frame.
// ------------------------------------------------------------------
namespace DungeonDash;

public enum GameState
{
    Menu,
    Playing,
    GameOver,
    Victory
}
